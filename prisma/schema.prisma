generator client {
  provider = "prisma-client-js"

datasource db {
  provider = "postgresql"
  // DO NOT add url or directUrl here
}

generator client {
  provider = "prisma-client-js"
}

model Audit {
  id        String   @id @default(cuid())
  url       String
  results   Json     // LH scores, custom checks
  summary   String?  // AI insights
  createdAt DateTime @default(now())
  userId    String?
}
